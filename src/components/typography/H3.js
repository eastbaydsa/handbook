import styled from "styled-components";
import { purple } from "../colors";

const H3 = styled.h3`
  color: ${purple};
  font-size: 18rem;
  font-weight: 700;
  margin: 0 0 20px 0;
`;

export default H3;
