import React from "react";
import styled from "styled-components";
import { red } from "../colors";

const H2 = styled.h2`
  color: ${red};
  font-size: 36px;
  font-weight: 600;
  margin: 0 0 20px 0;
`;

export default H2;
